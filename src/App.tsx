import React from 'react';
import { CryptoCurrency } from './components/CruptoCurrency';
import './App.scss'

export const App: React.FC = () => {
  return (
    <div>
      <CryptoCurrency />
    </div>
  );
}
